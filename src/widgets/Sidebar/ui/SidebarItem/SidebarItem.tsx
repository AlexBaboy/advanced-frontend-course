import React from 'react';

type SidebarItemProps = {
    className?: string
}

export const SidebarItem = ({className}: SidebarItemProps) => {
    return (
        <div>

        </div>
    );
};

export default SidebarItem;